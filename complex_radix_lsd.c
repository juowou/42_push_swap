#include "push_swap.h"
/*
int complex_radix_lsd(t_node **stack_a, t_node **stack_b)
{
	int     div;
	int     n_operations;

	n_operations = 0;
	div = 1;
	while (compute_disorder(stack_a) > 0)
	{
		printf("\ndiv = %d\ndisorder = %.2f\n\n", div, compute_disorder(stack_a));
		if ((*stack_a)->value < ((*stack_a)->next)->value)
		{
			operation_swap((*stack_a));
			ra(stack_a);
		}
		else
		{
			ra(stack_a);
		}
	}
	printf("\nnumber of operations = %d\n", n_operations);
	return (1);
}
	*/